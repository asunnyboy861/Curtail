# GLM-5.3-Flash secret profiles (gitignored — never commit the real file)
# Format: API_KEY|ENDPOINT_URL|MODEL_ID  (one profile per line, tried in order)
# International endpoint (best for US users): https://api.z.ai/api/paas/v4/chat/completions
# China fallback endpoint: https://open.bigmodel.cn/api/paas/v4/chat/completions
# Copy this file to GLMSecret.txt (in Curtail/) and fill in your key.
